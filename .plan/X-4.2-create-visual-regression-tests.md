# Task 4.2: Create visual regression tests

**Phase**: 4 - Playwright E2E + Visual Regression

**Description**: Create Playwright visual regression tests for Storybook stories

**Outcome**: `tests/visual/visual-regression.spec.ts` with screenshot comparison tests

**Implementation**:

Create `tests/visual/visual-regression.spec.ts`:
- Build Storybook static, iterate stories from `index.json`
- `toHaveScreenshot()` comparison for each story
- Baselines on first run, comparison on subsequent
