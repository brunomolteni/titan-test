# Task 4.1: Create E2E tests

**Phase**: 4 - Playwright E2E + Visual Regression

**Description**: Create Playwright E2E tests for the carousel component via Storybook iframe

**Outcome**: `tests/e2e/carousel.e2e.spec.ts` with comprehensive E2E tests

**Implementation**:

Create `tests/e2e/carousel.e2e.spec.ts`:
- Navigate to `/iframe.html?id=carousel--default`
- Press ArrowRight, assert `data-focused` attribute moves
- Assert `scrollLeft` changed (smooth scroll occurred)
- Boundary tests (Left at start, Right at end)
