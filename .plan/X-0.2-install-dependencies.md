# Task 0.2: Install dependencies

**Phase**: 0 - Project Scaffold

**Description**: Install all required dependencies for the project

**Outcome**: All packages installed via Bun

**Implementation**:

```bash
bun add react@^19 react-dom@^19 zustand
bun add -D @types/react @types/react-dom typescript \
  storybook@latest @storybook/react-vite @storybook/addon-vitest \
  @storybook/addon-interactions @storybook/addon-essentials @storybook/test \
  vitest @vitest/browser @playwright/test
```
