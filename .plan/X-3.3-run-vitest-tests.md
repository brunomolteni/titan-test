# Task 3.3: Run stories as Vitest browser tests

**Phase**: 3 - Storybook Stories + Vitest Interaction Tests

**Description**: Verify that Storybook stories run as Vitest browser tests via `@storybook/addon-vitest`

**Outcome**: Tests run successfully via `bun run test`

**Implementation**:

Run tests:
```bash
bun run test   # runs: vitest --project storybook
```

Verify that all stories execute as browser tests and play functions pass.
