# Task 3.2: Create CarouselItem.stories.tsx

**Phase**: 3 - Storybook Stories + Vitest Interaction Tests

**Description**: Create Storybook stories for CarouselItem component with interaction tests

**Outcome**: `src/components/Carousel/CarouselItem.stories.tsx` with stories and play function tests

**Implementation**:

Create `src/components/Carousel/CarouselItem.stories.tsx`:

Pure component, trivial stories:
- `Default`: `isFocused: false`, renders unfocused poster
- `Focused`: `isFocused: true`, renders with scale + focus ring
- `ClickSelect` play function: clicks item, asserts `onSelect` was called with the movie
