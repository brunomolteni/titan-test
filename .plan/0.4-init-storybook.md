# Task 0.4: Init Storybook 10

**Phase**: 0 - Project Scaffold

**Description**: Initialize Storybook 10 (ESM-only, uses Vite internally) and configure addon-vitest

**Outcome**: Storybook initialized with Vite builder and `@storybook/addon-vitest` configured

**Implementation**:

```bash
bunx storybook@latest init --builder vite
```

Update `.storybook/main.ts` to add `@storybook/addon-vitest` to the addons array.
